import React from 'react';
import PropTypes from 'prop-types';

import Styled from './workspace.css';

const Workspace = (props) => {
    return (
        <Styled.root>

        </Styled.root>
    );
}

export default Workspace;